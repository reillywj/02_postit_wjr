<% header = link_to('All Posts', posts_path) + " &raquo; #{@category.name}".html_safe %>

<%= render 'shared/content_title', title: header %>

<% @category.posts.sort{|x, y| x.total_votes <=> y.total_votes}.reverse.each do |post| %>
<%= render 'posts/post_preview', post: post %>
<% end %>

<% if @category.posts.empty? %>
  
<% end %>